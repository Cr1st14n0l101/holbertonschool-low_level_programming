#include "holberton.h"
#include <stdlib.h>
/**
 *  _abs - computes the absolute value of an integer
 * @i: the variable
 *  Return: Always 0 (Success)
 */
int _abs(int i)
{
if (i >= 0)
{
return (i);
}
return (-i);
}
